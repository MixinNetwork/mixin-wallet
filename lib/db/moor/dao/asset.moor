import '../mixin.moor';

assetResults(:fiatRate AS DOUBLE) as AssetResult:
SELECT a.*,
       tmp.icon_url AS chainIconUrl, (:fiatRate) as fiatRate
FROM assets a
LEFT JOIN assets tmp ON a.chain_id = tmp.asset_id;
